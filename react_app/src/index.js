// index.js

import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // あなたのメインのコンポーネント（Appコンポーネント）をインポートする

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
